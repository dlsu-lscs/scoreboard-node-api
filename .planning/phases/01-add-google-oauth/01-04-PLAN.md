---
phase: 01-add-google-oauth
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03-PLAN.md
files_modified:
  - index.js
autonomous: false
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-04
gap_closure: true
must_haves:
  truths:
    - "Google OAuth endpoints under /api/auth/* are reachable from Express (not 404/route miss)."
    - "A user who completes Google OAuth can call /api/scores/* with valid X-API-Key and session and receive success."
    - "After sign-out, previously working protected /api/scores/* requests fail session validation."
  artifacts:
    - path: "index.js"
      provides: "Prefix route mounting for better-auth so nested sign-in/callback/sign-out routes resolve."
      pattern: "app.all(\"/api/auth/*\", toNodeHandler(auth))"
  key_links:
    - from: "index.js"
      to: "config/auth.js"
      via: "import { auth } and mount with toNodeHandler(auth)"
      pattern: "import\s+\{\s*auth\s*\}\s+from\s+\"./config/auth.js\""
    - from: "index.js"
      to: "routes/scores.routes.js"
      via: "auth routes mounted before protected score routes"
      pattern: "app\.all\(\"/api/auth/\*\".*app\.use\(\"/api/scores\""
---

<objective>
Close the diagnosed Phase 1 UAT auth gap by fixing better-auth route mounting so Google OAuth session establishment works end-to-end.

Purpose: Unblock and pass UAT Test 5 (dual-token success) and Test 6 (logout invalidation) by restoring reachable auth endpoints.

Output:
- `index.js` mounts better-auth with prefix matching (`/api/auth/*`)
- OAuth sign-in/callback/sign-out endpoints are reachable for session creation/invalidation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-add-google-oauth/01-UAT.md
@.planning/phases/01-add-google-oauth/01-03-SUMMARY.md
@index.js
@config/auth.js
@middlewares/auth.middleware.js

<interfaces>
From index.js:
```javascript
import { toNodeHandler } from "better-auth/node";
import { auth } from "./config/auth.js";
app.all("/api/auth/", toNodeHandler(auth));
app.use("/api/scores", scoresRouter);
```

From middlewares/auth.middleware.js:
```javascript
export async function validateSession(req, res, next) {
  const session = await auth.api.getSession({ headers: fromNodeHeaders(req.headers) });
  if (!session) return res.status(401).json({ message: "Unauthorized: Invalid or expired session." });
  req.user = session.user;
  req.session = session.session;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix better-auth mount to prefix route matching</name>
  <files>index.js</files>
  <action>
    Update the auth mount from exact path `app.all("/api/auth/", toNodeHandler(auth))` to prefix/wildcard matching `app.all("/api/auth/*", toNodeHandler(auth))` so nested better-auth endpoints (`/sign-in/social`, `/callback/google`, `/sign-out`, `/session`) are reachable. This directly addresses UAT root cause and implements D-09 (mount better-auth at `/api/auth/*`).

    Keep middleware order unchanged: body parsers first, auth mount second, protected `/api/scores` routes after auth mount.
  </action>
  <verify>
    <automated>node --check index.js && node -e "const fs=require('fs');const s=fs.readFileSync('index.js','utf8');if(!s.includes('app.all(\"/api/auth/*\", toNodeHandler(auth))')){console.error('missing wildcard auth mount');process.exit(1)}"</automated>
  </verify>
  <done>index.js uses wildcard auth mount and no syntax errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run blocked OAuth/session UAT flow</name>
  <files>index.js</files>
  <action>
    Execute targeted UAT re-validation for the diagnosed gap after Task 1. Confirm that better-auth nested routes are reachable, complete OAuth login, and verify protected `/api/scores/*` behavior before and after logout. Update UAT evidence so the gap can be closed explicitly.
  </action>
  <what-built>Auth endpoint routing now supports nested better-auth OAuth/session routes needed for login/logout lifecycle.</what-built>
  <how-to-verify>
    1) Start server from cold state: `npm run start`.
    2) Verify auth route is reachable (not route-miss):
       - `curl -i "http://localhost:3000/api/auth/session"`
       - Expect response from better-auth (e.g., 200/401), and NOT Express 404 for missing route.
    3) Complete Google OAuth login in browser using configured client credentials.
    4) Call a protected endpoint with valid `X-API-Key` and established session; expect success (UAT Test 5 truth).
    5) Call sign-out, then repeat protected endpoint call; expect session auth failure 401/403 (UAT Test 6 truth).
    6) Record results back into `01-UAT.md` as pass/fail with evidence.
  </how-to-verify>
  <verify>
    <automated>curl -s -o NUL -w "%{http_code}" "http://localhost:3000/api/auth/session"</automated>
  </verify>
  <done>
    - UAT Test 5 moves from issue -> pass.
    - UAT Test 6 moves from blocked -> pass.
    - OAuth redirect/login path is no longer blocked by route mounting.
  </done>
  <resume-signal>Type "approved" after confirming results, or paste failing step + response.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/auth/*` | Untrusted client requests enter auth endpoints for session create/read/invalidate |
| `/api/auth/*` -> Google OAuth | Third-party identity provider callback enters backend |
| Session state -> `/api/scores/*` | Session/auth context gates access to protected score data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | S | OAuth callback route | mitigate | Ensure callback endpoint is reachable only through better-auth mount and provider/state validation in better-auth handler |
| T-01-04-02 | T | Auth route matching | mitigate | Use explicit `/api/auth/*` mount to prevent accidental bypass/misrouting of nested auth endpoints |
| T-01-04-03 | R | Login/logout events | accept | No new audit log in this gap closure; acceptable for this targeted fix because no new PII surface introduced |
| T-01-04-04 | I | Session introspection endpoint | mitigate | Keep session protected by better-auth internals and existing cookie/session validation path |
| T-01-04-05 | D | Auth endpoint abuse | accept | Rate limiting is already deferred to later phase per context; no scope expansion in gap closure |
| T-01-04-06 | E | Protected score access | mitigate | Preserve dual-token gate (`validateApiKey` + `validateSession`) and verify post-login success + post-logout failure |
</threat_model>

<verification>
- `node --check index.js` passes.
- `curl -i http://localhost:3000/api/auth/session` returns a better-auth handler response (not missing-route 404).
- UAT Test 5 evidence shows protected endpoint succeeds with valid API key + authenticated session.
- UAT Test 6 evidence shows protected endpoint fails after logout.
</verification>

<success_criteria>
- Root-cause mount mismatch in `index.js` is corrected to `/api/auth/*`.
- OAuth login is no longer blocked by unreachable nested auth routes.
- Dual-token success and logout invalidation truths are verified and documented in UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/01-add-google-oauth/01-04-SUMMARY.md`
</output>
